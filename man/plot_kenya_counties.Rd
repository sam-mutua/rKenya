% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_kenya_counties.R
\name{plot_kenya_counties}
\alias{plot_kenya_counties}
\title{Plot Kenya County Boundaries}
\usage{
plot_kenya_counties(
  data = NULL,
  fill_var = NULL,
  join_by = "county",
  title = NULL
)
}
\arguments{
\item{data}{An optional data frame to join onto the county boundaries.
Must contain a column matching \code{join_by} with county names, plus
the variable named in \code{fill_var}.}

\item{fill_var}{A string giving the column in \code{data} to use as the
fill variable. Ignored when \code{data} is \code{NULL}.}

\item{join_by}{A string giving the county name column in \code{data} used
to join with the \code{COUNTY} field in \code{kenya_map}.
Defaults to \code{"county"}.}

\item{title}{Optional plot title string.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a \code{ggplot2} map of Kenya's 47 counties. Optionally fill
counties by a numeric variable to produce a map.
}
\examples{
\donttest{
# Map: pass a data frame with a numeric column
df <- data.frame(
  county = ke_shape$COUNTY,
  value  = runif(47, 0, 100)
)
plot_kenya_counties(data = df, fill_var = "value", title = "Random values")
}

}
